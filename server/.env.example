# Template for server/.env — copy this file to server/.env and fill in values.
# server/.env holds local secrets layered on top of server/debug.env; all keys are optional.

# Enables the location picker on public-facing forms and the events location picker.
# See README for details.
#GOOGLE_PLACES_API_KEY=
